steps:
  - bash: |
      if [[ -n "${CHECKOUT_TAG}" ]]; then
        az login --identity
        echo ${CHECKOUT_TAG}
        az acr repository delete -n $(registry.name) --image oss/openservicemesh/osm-chart:${CHECKOUT_TAG} --yes
      else
        echo "Helm chart was not published to staging ACR because CHECKOUT_TAG variable failed to be created before packaging helm chart" 
      fi
    displayName: Delete helm chart from staging registry
    workingDirectory: $(repo.path)
    condition: always()
